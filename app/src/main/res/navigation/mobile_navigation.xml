<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/calendar_fragment">

    <fragment
        android:id="@+id/calendar_fragment"
        android:name="com.catp.thundersimlineup.ui.calendar.CalendarFragment"
        android:label="@string/title_home"
        tools:layout="@layout/calendar_fragment" />

    <fragment
        android:id="@+id/lineup_list_fragment"
        android:name="com.catp.thundersimlineup.ui.lineuplist.LineupListFragment"
        android:label="@string/title_dashboard"
        tools:layout="@layout/lineup_list" />

    <fragment
        android:id="@+id/navigation_notifications"
        android:name="com.catp.thundersimlineup.ui.notifications.NotificationsFragment"
        android:label="@string/title_notifications"
        tools:layout="@layout/fragment_notifications" />
</navigation>